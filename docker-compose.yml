services:
  aggregator:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - rss_data:/data
    environment:
      - ADMIN_USER
      - ADMIN_PASS
      - FEED_USER
      - FEED_PASS
      - DB_PATH=/data/cache.db
    restart: unless-stopped

volumes:
  rss_data: